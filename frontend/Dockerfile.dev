FROM node:22-alpine
LABEL org.opencontainers.image.title="Canvas To Do (development frontend)"

WORKDIR /app

COPY frontend/package*.json ./
RUN npm install

EXPOSE 6173

CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 6173"]
